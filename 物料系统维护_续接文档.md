# 物料系统维护续接文档

## 给 ChatGPT / Codex 的开场说明
本文档是 `MaterialSystem` 后续维护的主要续接依据。新的对话、新账号、新 Codex 任务开始前，请先读本文档，再读当前项目代码。若本文档与代码不一致，以代码为准，确认后再更新本文档。每次 Codex 修改代码或完成测试后，都必须在本文档末尾追加一条“本次修改记录”。

## 项目基本信息
- 项目名称：润微物料管理系统 / 物料管理系统
- 项目根目录：`C:\Users\admin\Desktop\MaterialSystem`
- 主入口：`app.py`
- 启动脚本：`streamlit-startup.bat`
- 常用启动方式：
  - `.\venv\Scripts\streamlit.exe run .\app.py`
  - 或双击 `streamlit-startup.bat`
- 浏览器访问地址：以当前系统端口为准，通常是 [http://localhost:8501](http://localhost:8501/)
- 主要技术：Streamlit + MySQL + Excel 生成 + 飞书同步相关脚本
- 重要说明：`config.py` 可能包含本地配置或敏感配置，不上传 GitHub，不随意修改

## 核心文件结构
- `app.py`：Streamlit 主入口，负责页面组织、登录态和模块加载
- `database.py`：数据库连接、表结构初始化、通用读写
- `config.py`：本地配置和敏感配置
- `business_logic.py`：主要业务逻辑
- `excel_engines.py`：各类 Excel 生成逻辑，当前辅料下单 Excel 也在这里
- `feishu_sync.py`：飞书相关同步基础能力
- `sync_feishu_to_mysql.py`：飞书数据同步到 MySQL
- `sync_mysql_to_feishu.py`：MySQL 数据同步到飞书
- `sync_history_to_feishu.py`：历史记录同步到飞书
- `utils.py`：通用工具函数
- `modules/accessory.py`：辅料下单页面
- `modules/purchase.py`：采购合同下单页面
- `modules/outbound.py`：出库相关页面
- `modules/inbound.py`：入库相关页面
- `modules/history.py`：历史记录页面
- `modules/admin.py`：后台管理页面
- `modules/dashboard.py`：看板页面
- `modules/feishu_portal.py`：飞书采购申请入口相关页面
- `streamlit-startup.bat`：Windows 双击启动脚本

## 开发原则
1. 系统已上线使用，优先最小改动、低风险。
2. 不要重构整个项目。
3. 不要删除已有功能。
4. 不要随意修改数据库结构。
5. 不要随意修改 `config.py`。
6. 不要提交 `venv/`、`images/`、数据库文件、Excel / CSV 导出、`*.pyc`、缓存文件。
7. 历史遗留问题优先考虑数据库补状态或一次性脚本，不优先加新按钮。
8. 每次修改前先 `git status`。
9. 每次修改后先测试，再 commit。
10. 每次 Codex 修改代码或完成测试，都必须更新本文档。
11. 修改完成后必须写清楚修改文件、修改内容、测试步骤、当前测试状态和下一步建议。

## 当前 Git / GitHub 状态
- Git 已初始化
- GitHub private repository：<https://github.com/fzs040617/MaterialSystem.git>
- `config.py` 已被 `.gitignore` 忽略，不上传
- `venv/`、`images/`、Excel / CSV、数据库等业务文件不应上传
- 修改前后建议运行：
  ```powershell
  git status
  ```

## 已完成的重要修改记录
1. 采购合同单价显示精度修复
   - 单价 `0.0035` 不应显示为 `0.004`
   - 页面格式已调整为更高精度

2. `purchase_orders.remark` 字段过短修复
   - 已通过脚本或 `ALTER TABLE` 将 `remark` 调整为 `LONGTEXT`

3. 飞书采购申请旧记录重复显示修复
   - 已通过补写 `migrated_feishu_records` 处理旧记录
   - 不增加页面按钮

4. `cryptography` 依赖修复
   - 已安装 `cryptography`
   - `requirements.txt` 已加入对应版本

5. GitHub 备份
   - 已创建 private repository
   - 已上传初始代码
   - `config.py` 未上传

6. 辅料下单第一轮修改
   - 修改文件：`modules/accessory.py`、`excel_engines.py`
   - 页面新增 / 调整：`采购单查询码（必填）`、`洗水唛材料`、`是否两件装`、辅料款式改为 `绿色吊牌+吊粒` / `五张新吊牌+防伪带` / `贴纸`
   - Excel 生成逻辑：
     - 读取旺店通采购单导出表
     - 删除货品名称中的 `(VIP)` / `（VIP）`
     - 吊牌采购量 = 采购确认量
     - 洗水唛采购量按洗水唛和两件装规则计算
     - 底部写入吊牌总采购量、洗水唛总采购量、收件信息
     - 右侧写入完整执行标准模板

## 当前辅料下单需求状态
1. 原表单来自旺店通采购单管理导出表。
2. 当前第一轮仍保留上传入口测试，不直接接 RPA。
3. 后续 RPA 需要限制：
   - 09:00-10:00 禁止查询
   - 14:00-15:00 禁止查询
   - 同一时间只允许一个用户查询
4. 后续需要自动附辅料图片：
   - `绿色吊牌+吊粒`
   - `五张新吊牌+防伪带`
   - `贴纸` 待确认
5. 地址逻辑和包装袋下单通用，生成表单时使用页面选择收货制衣厂后显示的完整收件信息。
6. 69 码来源暂未确认，当前 `69码=有` 时先生成空列。
7. 洗水唛不用放图。

## 当前已知的 Excel 规则
- 主表明细基于旺店通原表单生成
- 采购量列为显示列，若原表中存在则显示原值，不参与总量计算
- 计算全部以 `采购确认量` 为准
- 吊牌采购量 = 采购确认量
- 洗水唛采购量：
  - 洗水唛 = 无：0
  - 洗水唛 = 有 且 是否两件装 = 否：采购确认量
  - 洗水唛 = 有 且 是否两件装 = 是：采购确认量 × 2
- 货品名称需要删除 `(VIP)` / `（VIP）`
- `规格码` 需要做右侧模板显示清洗，去掉 Excel 公式式写法
- 商品价右侧展示为 `￥金额`
- 右侧执行标准区域需要完整模板，不只是一个标准号
- 主表合计行 / 汇总行需要过滤，不参与明细和数量统计
- 生成结果需要避免出现 `nan` / `None` 字符串
- 主表列宽需要紧凑可读，全表默认微软雅黑
- 右侧执行标准区域字号会适当放大，方便打印和检查

## 看板包装袋图片路径说明
- 包装袋下单看板的图片资源继续沿用项目内现有路径
- 图片文件仍放在 `images/` 目录下管理
- 维护时不要把图片路径、图片文件名改散到多个地方
- 如果后续需要替换图片，优先保持路径约定不变，只更新图片文件本身

## 后续维护注意事项
1. 所有改动优先最小范围、低风险。
2. 改动前先看 `git status`，确认当前变更面。
3. 改动后先做编译或页面测试，再考虑提交。
4. 不要提交 `rpa_runtime/`、导出的 Excel / CSV、临时文件、缓存文件。
5. RPA 相关只保留必要接口和状态文件，不要在本轮随意扩大自动化边界。
6. 如果文档和代码出现不一致，先读代码确认，再更新文档。
7. 每次维护完成后，补一条新的“本次修改记录”。

## 测试记录模板
```markdown
## YYYY-MM-DD 本次修改记录

### 本次完成

写清楚完成了什么功能。

### 修改文件

逐个列出文件路径和修改内容。

### 新增 / 修改逻辑

说明页面、Excel、脚本、接口或数据库变化。

### 测试步骤

1. 如何启动系统。
2. 打开哪个页面。
3. 输入什么测试数据。
4. 点击什么按钮。
5. 预期结果是什么。
6. 失败时应该把什么报错发回来。

### 当前测试状态

已测试通过 / 未测试 / 部分通过 / 有遗留问题。

### 下次继续建议

写清楚下一步应该从哪里继续。
```

## 本次修改记录

### 本次完成

本次只重写并更新了《物料系统维护_续接文档.md》，把当前项目状态、辅料下单规则、RPA 限制、看板图片说明和后续维护边界收拢到一份可续接的文档里。

### 修改文件

- `物料系统维护_续接文档.md`
  - 重写为当前状态版续接文档
  - 补充项目路径、启动方式、GitHub 仓库、核心文件说明、开发原则、辅料下单现状和测试模板

### 新增 / 修改逻辑

本次只改文档，不改业务代码、不改数据库、不改 `config.py`、不改 `app.py`、不改 `business_logic.py`。

### 测试步骤

1. 打开项目根目录。
2. 查看 `物料系统维护_续接文档.md`。
3. 确认文档包含当前项目状态、辅料下单规则、维护边界和测试模板。
4. 执行 `git status`，确认仅文档发生变化。

### 当前测试状态

已完成文档更新，未做页面联调测试。

### 下次继续建议

下一步优先做辅料下单 Excel 的真实样本联调，重点确认 RPA 结果接入口、表头与右侧模板展示是否稳定。
