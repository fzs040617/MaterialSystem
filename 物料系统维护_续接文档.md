## 系统完整说明

### 一、系统一句话说明

本项目是已经上线使用的润微物料管理系统，用于包装袋下单、辅料下单、采购合同下单、其他物料下单、入库、制衣厂消耗、跨境物料管理、飞书采购申请同步、飞书与 MySQL 数据同步、历史记录查看、后台管理、看板统计以及各类 Excel 表单生成。

本系统不是测试 Demo，而是正在使用的业务系统。后续无论由 ChatGPT、Codex 还是其他开发者接手，都必须优先保证现有业务流程稳定，不得随意重构、删除功能或大范围改动数据库结构。

### 二、页面模块总览

系统主要页面 / Tab 包括：

* 看板：查看系统关键业务数据和统计信息。
* 采购合同下单：生成采购合同相关表单，涉及采购订单、供应商、物料明细和合同 Excel。
* 其他物料下单：处理包装袋之外的其他物料下单流程。
* 包装袋下单：选择收货制衣厂、商品、规格和数量，加入清单并生成下单表；其收货地址逻辑后续被辅料下单复用。
* 辅料下单：根据旺店通采购单管理导出的原表单生成辅料下单表，当前正在改造中。
* 入库：处理物料入库相关流程。
* 制衣厂消耗：记录或查看制衣厂物料消耗情况。
* 跨境物料：处理跨境物料相关业务。
* 飞书采购申请：同步和处理飞书采购申请。
* 历史：查看下单、入库、撤销、同步等历史记录。
* 后台：管理系统配置、用户或基础数据等后台功能。

这些模块之间存在库存、历史、飞书同步、Excel 生成等关联，修改时不能只看单个页面，必须注意对原流程的影响。

### 三、核心文件作用说明

* `app.py`：Streamlit 主入口，负责页面配置、Tab 组织、初始化数据库、登录状态和模块加载。
* `database.py`：数据库连接、表结构初始化、表结构修复、数据读取和保存等。
* `config.py`：本地配置和敏感配置文件，可能包含数据库、飞书等配置；已被 `.gitignore` 忽略，不上传 GitHub，非必要不修改。
* `business_logic.py`：主要业务逻辑，包括登录注册、草稿箱、下单扣库存、撤销订单、撤销采购合同等。
* `excel_engines.py`：各种 Excel 表单生成逻辑，当前辅料下单新表单生成逻辑也在这里。
* `feishu_sync.py`：飞书 token 获取、飞书数据拉取、图片下载等飞书基础能力。
* `sync_feishu_to_mysql.py`：将飞书基础数据同步到 MySQL。
* `sync_mysql_to_feishu.py`：将 MySQL 数据反向同步到飞书。
* `sync_history_to_feishu.py`：将本地历史记录推送到飞书历史表。
* `utils.py`：通用工具函数，例如人民币大写、列宽调整、hash、文件名清洗等。
* `modules/accessory.py`：辅料下单页面。
* `modules/purchase.py`：采购合同下单页面。
* `modules/outbound.py`：包装袋 / 物料出库或下单相关页面。
* `modules/inbound.py`：入库页面。
* `modules/history.py`：历史记录页面。
* `modules/admin.py`：后台管理页面。
* `modules/dashboard.py`：看板页面。
* `modules/feishu_portal.py`：飞书采购申请页面。
* `streamlit-startup.bat`：Windows 双击启动脚本，用于启动 Streamlit 系统。

### 四、数据和安全边界

以下内容不要上传 GitHub：

* `config.py`
* `venv/`
* `images/`
* Excel / CSV 业务数据
* 数据库文件
* `*.pyc`
* `__pycache__/`
* `.env`
* 临时下载文件
* 真实业务图片和表格

GitHub 仓库虽然是 private repository，但仍然不要上传数据库密码、飞书密钥、token、cookie、真实业务数据或系统敏感配置。

每次修改前后都应执行：

```powershell
git status
```

确认没有误加入 `config.py`、数据库、Excel、CSV、图片、虚拟环境或缓存文件。

### 五、当前辅料下单最新需求状态

辅料下单正在从旧逻辑改造为面向旺店通采购单管理导出原表单的新流程。

当前确认规则如下：

1. 原表单来自旺店通采购单管理导出，不再使用厂家表单。
2. 当前第一轮仍保留上传入口测试，不真正接 RPA。
3. 后续接 RPA 时，需要限制：

   * 09:00-10:00 禁止查询；
   * 14:00-15:00 禁止查询；
   * 同一时间只允许一个用户查询；
   * 如果已有用户正在查询，其他用户提示稍后再试。
4. 旺店通原表单只读取 9 列：

   * 商家编码
   * 货品编号
   * 货品名称
   * 规格名称
   * 规格码
   * 采购量
   * 采购确认量
   * 零售价格
   * 平台
5. 采购单查询码必填。
6. 货品名称需要删除 `(VIP)` / `（VIP）`。
7. 吊牌采购量 = 采购确认量。
8. 吊牌总采购量 = 采购确认量总和。
9. 新增洗水唛采购量。
10. 洗水唛 = 无：洗水唛采购量为 0。
11. 洗水唛 = 有，且非两件装：洗水唛采购量 = 采购确认量。
12. 洗水唛 = 有，且两件装：洗水唛采购量 = 采购确认量 × 2。
13. 洗水唛总采购量 = 洗水唛采购量总和。
14. 洗水唛材料二选一：

    * 胶带（唯品/三野）
    * 布带（天猫/抖音）
15. 洗水唛不用放图。
16. 是否两件装默认“否”。
17. 辅料款式改为：

    * 绿色吊牌+吊粒
    * 五张新吊牌+防伪带
    * 贴纸
18. 后续需要给“绿色吊牌+吊粒”和“五张新吊牌+防伪带”自动附图，但第一轮暂未实现。
19. 地址逻辑和包装袋下单通用，生成表单时写入用户选择收货制衣厂后显示的完整收件信息。
20. 右侧执行标准模板不是只写标准号，而是完整模板：

    * 润微
    * 名称：品类（款名）
    * 货号
    * 号型
    * 颜色
    * 商品价
    * 执行标准
21. 名称示例：`舒适文胸（风栖于梧）`。
22. 货号、号型、颜色、商品价取原表单第一行。
23. 品类识别要长词优先，例如“舒适文胸”优先于“文胸”。
24. 69码来源暂未确认，当前 69码=有 时先生成空列。

### 六、当前已完成修改和测试状态

当前辅料下单第一轮代码已经由 Codex 修改。

修改文件：

```text
modules/accessory.py
excel_engines.py
```

已完成：

* “内部码（选填）”改为“采购单查询码（必填）”。
* 采购单查询码为空时提示“请先填写采购单查询码”。
* 洗水唛为“有”时显示洗水唛材料。
* 新增“是否两件装”，默认“否”。
* 辅料款式改为“绿色吊牌+吊粒 / 五张新吊牌+防伪带 / 贴纸”。
* Excel 读取旺店通 9 列。
* 删除货品名称中的 VIP。
* 生成吊牌采购量和洗水唛采购量。
* 底部写入吊牌总采购量、洗水唛总采购量、收件信息。
* 右侧写入完整执行标准模板。

已通过 Python 编译检查：

```powershell
.\venv\Scripts\python.exe -m py_compile .\modules\accessory.py .\excel_engines.py
```

页面已能看到：

* 采购单查询码（必填）
* 是否两件装
* 新辅料款式

仍需继续测试：

* 上传旺店通原表单后生成 Excel。
* 检查吊牌采购量、洗水唛采购量、底部总量、收件信息和右侧执行标准模板是否完全正确。
* 测试洗水唛=无、有、两件装三种情况。
* 测试 69码=有 时是否新增空列。

### 七、每次修改记录机制

以后每次 Codex 修改代码、做测试、修 bug、改启动脚本、改文档，都必须在本文档末尾追加“本次修改记录”。

每次记录必须包含：

```markdown
## YYYY-MM-DD 本次修改记录

### 本次完成

写清楚完成了什么功能。

### 修改文件

逐个列出文件路径和修改内容。

### 新增 / 修改逻辑

说明页面、Excel、脚本、接口或数据库变化。

### 测试步骤

1. 如何启动系统。
2. 打开哪个页面。
3. 输入什么测试数据。
4. 点击什么按钮。
5. 预期结果是什么。
6. 失败时应该把什么报错发回来。

### 当前测试状态

已测试通过 / 未测试 / 部分通过 / 有遗留问题。

### 下次继续建议

写清楚下一步应该从哪里继续。
```

### 八、2026-06-16 本次修改记录：补充系统完整说明

#### 本次完成

补充《物料系统维护_续接文档.md》的系统完整说明，使新的 ChatGPT / Codex 只读本文档即可理解 MaterialSystem 是什么系统、有哪些页面模块、核心文件负责什么、当前辅料下单需求进展到哪里，以及后续维护应该遵守哪些规则。

#### 修改文件

```text
物料系统维护_续接文档.md
```

#### 新增 / 修改逻辑

本次只补充文档，不修改业务代码、不修改启动脚本、不修改数据库、不修改 `config.py`、`database.py`、`app.py`。

#### 测试步骤

1. 打开项目根目录。
2. 打开 `物料系统维护_续接文档.md`。
3. 检查文档是否包含系统完整说明、页面模块总览、核心文件说明、数据和安全边界、当前辅料下单状态、每次修改记录机制。
4. 执行 `git status`，确认本次没有新增业务代码改动。

#### 当前测试状态

文档待用户检查。

#### 下次继续建议

下一步继续测试辅料下单生成 Excel 的完整结果。测试通过后再执行 Git 提交和推送。
# 物料系统维护续接文档

## 一、给 ChatGPT / Codex 的开场说明

本文档是润微物料管理系统后续维护的主要续接依据。

新的 ChatGPT 对话、新账号、新 Codex 任务开始前，必须先读本文档，再读当前项目代码。本文档不替代代码；如果文档和代码不一致，先读代码确认真实状态，再更新本文档。

每次 Codex 修改代码、调整脚本或完成测试后，都必须在本文档末尾追加“本次修改记录”，写清楚修改文件、修改内容、测试步骤、当前测试状态和下一步建议。

## 二、项目基本信息

- 项目名称：润微物料管理系统 / 物料管理系统
- 项目根目录：`C:\Users\admin\Desktop\MaterialSystem`
- 主入口：`app.py`
- 启动脚本：`streamlit-startup.bat`
- 常用启动方式：
  - `.\venv\Scripts\streamlit.exe run .\app.py --server.port 8501`
  - 双击 `streamlit-startup.bat`
- 浏览器访问地址：[http://localhost:8501](http://localhost:8501/)
- 主要技术：Streamlit + MySQL + Excel 生成 + 飞书同步相关脚本
- 重要说明：`config.py` 含本地配置或敏感配置，不上传 GitHub，不随意修改。

## 三、核心文件结构

- `app.py`：Streamlit 主入口，负责登录、角色判断和页面 tab 装载。
- `database.py`：数据库连接、表初始化、通用数据读取等基础能力。
- `config.py`：本地配置和敏感配置文件，已被 `.gitignore` 忽略。
- `business_logic.py`：包装袋、采购、库存等核心业务逻辑。
- `excel_engines.py`：Excel 生成引擎，包含包装袋、辅料、采购合同、库存报表等导出逻辑。
- `feishu_sync.py`：飞书相关同步入口或兼容脚本。
- `sync_feishu_to_mysql.py`：飞书数据同步到本地 MySQL。
- `sync_mysql_to_feishu.py`：本地 MySQL 数据反向同步到飞书。
- `sync_history_to_feishu.py`：历史记录同步到飞书多维表格。
- `utils.py`：通用工具函数，例如 Excel 列宽、金额大写转换等。
- `modules/accessory.py`：辅料下单页面。
- `modules/purchase.py`：采购合同下单页面。
- `modules/outbound.py`：包装袋下单、其他物料下单等出库页面。
- `modules/inbound.py`：入库、制衣厂消耗登记页面。
- `modules/history.py`：历史记录查询、下载、撤销等页面。
- `modules/admin.py`：后台管理页面，包含基础资料维护和同步入口。
- `modules/dashboard.py`：看板页面。
- `modules/feishu_portal.py`：飞书采购申请入口或相关页面。
- `streamlit-startup.bat`：Windows 双击启动脚本，优先使用项目内虚拟环境启动 Streamlit。

## 四、开发原则

1. 系统已上线使用，优先最小改动、低风险。
2. 不要重构整个项目。
3. 不要删除已有功能。
4. 不要随意修改数据库结构。
5. 不要随意修改 `config.py`。
6. 不要提交 `venv`、`images`、数据库、Excel、CSV、`pyc`、缓存文件。
7. 历史遗留问题优先考虑数据库补状态或一次性脚本，不优先加新按钮。
8. 每次修改前先运行 `git status`。
9. 每次修改后先测试，再 commit。
10. 每次 Codex 修改代码或完成测试，都必须更新本文档。
11. 修改完成后必须写清楚修改文件、修改内容、测试步骤、当前测试状态和下一步建议。

## 五、当前 Git / GitHub 状态

- 已初始化 Git。
- GitHub private repository：[https://github.com/fzs040617/MaterialSystem.git](https://github.com/fzs040617/MaterialSystem.git)
- `config.py` 已被 `.gitignore` 忽略，不上传。
- `venv`、`images`、Excel、CSV、数据库等不应上传。
- 修改前后建议运行：

```bash
git status
```

## 六、已完成的重要修改记录

1. 采购合同单价显示精度修复
   - 单价 `0.0035` 不应显示为 `0.004`。
   - 页面格式从 `%.3f` 调整为更高精度。

2. `purchase_orders.remark` 字段过短修复
   - 通过 `fix_purchase_remark.py` 或 `ALTER TABLE` 将 `remark` 调整为 `LONGTEXT`。

3. 飞书采购申请旧记录重复显示修复
   - 使用 `mark_old_feishu_requests_done.py` 补写 `migrated_feishu_records`。
   - 不加页面按钮，避免给线上用户增加误操作入口。

4. `cryptography` 依赖修复
   - 已安装 `cryptography`。
   - `requirements.txt` 已加入 `cryptography` 版本。

5. GitHub 备份
   - 已创建 private repository。
   - 已上传初始代码。
   - `config.py` 未上传。

6. 辅料下单第一轮修改
   - 修改文件：`modules/accessory.py`、`excel_engines.py`。
   - 页面新增 / 修改：采购单查询码（必填）、洗水唛材料、是否两件装。
   - 辅料款式改为：绿色吊牌+吊粒、五张新吊牌+防伪带、贴纸。
   - Excel 生成逻辑：读取旺店通 9 列，删除 VIP，吊牌采购量 = 采购确认量。
   - 洗水唛采购量按洗水唛和两件装规则计算。
   - 底部写吊牌总采购量、洗水唛总采购量、收件信息。
   - 右侧写完整执行标准模板。

## 七、当前辅料下单需求状态

1. 原表单来自旺店通采购单管理导出表。
2. 当前第一轮暂时仍使用上传入口测试，不接 RPA。
3. 后续 RPA 需要限制：
   - 09:00-10:00 禁止查询。
   - 14:00-15:00 禁止查询。
   - 同一时间只允许一个用户查询，其他用户提示“当前已有用户正在查询采购单，请稍后再试。”
4. 后续需要自动附辅料图片：
   - 绿色吊牌+吊粒。
   - 五张新吊牌+防伪带。
   - 贴纸待确认。
5. 地址逻辑和包装袋下单通用。
6. 69码来源暂未确认，当前 69码=有 时先生成空列。
7. 洗水唛不用放图。

## 八、测试记录模板

以后每次维护后，在文档末尾按以下格式追加：

```markdown
## YYYY-MM-DD 本次修改记录

### 本次完成

写清楚完成了什么功能。

### 修改文件

逐个列出文件路径和修改内容。

### 新增 / 修改逻辑

说明页面、Excel、脚本、接口或数据库变化。

### 测试步骤

1. 如何启动系统。
2. 打开哪个页面。
3. 输入什么测试数据。
4. 点击什么按钮。
5. 预期结果是什么。
6. 失败时应该把什么报错发回来。

### 当前测试状态

已测试通过 / 未测试 / 部分通过 / 有遗留问题。

### 下次继续建议

写清楚下一步应该从哪里继续。
```

## 九、2026-06-16 本次修改记录

### 本次完成

本次完成两个低风险维护事项：优化 `streamlit-startup.bat`，新增 `物料系统维护_续接文档.md`。本次不修改业务代码，不修改数据库，不修改 `config.py`。

### 修改文件

- `streamlit-startup.bat`
  - 改为以 bat 自身所在目录作为项目根目录。
  - 进入项目目录后启动系统。
  - 优先执行 `.\venv\Scripts\streamlit.exe run .\app.py --server.port 8501`。
  - 如果 `streamlit.exe` 不存在，则尝试 `.\venv\Scripts\python.exe -m streamlit run .\app.py --server.port 8501`。
  - 如果 venv 不存在或 Streamlit 退出失败，输出英文错误提示并 `pause`，避免窗口闪退。

- `物料系统维护_续接文档.md`
  - 新增项目续接说明、基本信息、核心文件结构、开发原则、Git/GitHub 状态、重要修改记录、辅料下单需求状态、测试记录模板和本次修改记录。

### 新增 / 修改逻辑

- 仅修改 Windows 启动脚本和维护文档。
- 未修改页面业务逻辑。
- 未修改 Excel 生成逻辑。
- 未修改数据库结构。
- 未接 RPA。
- 未插入辅料图片。

### 测试步骤

1. 双击 `streamlit-startup.bat`。
2. 或在项目目录运行 `.\streamlit-startup.bat`。
3. 确认命令行显示 `Starting MaterialSystem...`。
4. 确认系统可打开。
5. 确认 [http://localhost:8501](http://localhost:8501/) 可访问。
6. 确认根目录存在 `物料系统维护_续接文档.md`，并包含本次记录。
7. 如果失败，把命令行窗口中的完整错误提示发回。

### 当前测试状态

未做实际启动测试；本次仅完成脚本静态检查和文档落地。启动测试需要在本机已配置好 venv 和 Streamlit 的环境中执行。

### 下次继续建议

优先双击测试 `streamlit-startup.bat`。确认启动脚本稳定后，再继续辅料下单第二轮：旺店通 RPA 查询限制、69码来源确认、辅料图片模板保存与插入。
